const li = document.querySelector('li')

const styles = getComputedStyle(li)
// styles va a ser un objeto con todas las declaraciones
// posibles de CSS. la mayoría con los valores default del 
// navegador
 